# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'eraser' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for eraser
  pod 'MJExtension'
  pod 'Masonry'

  target 'eraserTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'eraserUITests' do
    # Pods for testing
  end

end
